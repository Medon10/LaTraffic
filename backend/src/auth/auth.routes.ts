import { Router } from 'express';

const router = Router();

// Rutas de autenticación — se implementan en T-04
// POST /auth/registro
// POST /auth/login
// POST /auth/recuperar-password
// POST /auth/reset-password

export { router as authRoutes };
